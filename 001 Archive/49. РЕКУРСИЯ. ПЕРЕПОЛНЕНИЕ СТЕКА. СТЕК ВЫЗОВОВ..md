---
tags:
  - csharp
date: 2025-08-20
---
[ЧТО ТАКОЕ РЕКУРСИЯ C# | ПЕРЕПОЛНЕНИЕ СТЕКА | СТЕК ВЫЗОВОВ | C# ОТ НОВИЧКА К ПРОФЕССИОНАЛУ | # 49](https://youtu.be/B50p1BRyb_E?si=Z9L-EO3HYXkGP8sv)

### Что такое рекурсия

*Рекурсия* (рекурсивный вызов)- это вызов метода самим себя.

```csharp
        static void Foo()
        {
            Console.WriteLine("Foo");
            Foo();
        }
        static void Main(string[] args)
        {
            Foo();
        }
```

- По сути это как бесконечный цикл

- Всё, что можно реализовать циклом, можно реализовать рекурсией.

-  Рекурсия ограничена размером стека.

#### Посмотреть стек вызовов можно если:
- В дэбаге  нажать *отладка* - *окна* - *показать стек вызовов* (Ctrl Alt C)

![[Pasted image 20250820152037.png]]

#### Так отображается стек: 

![[Pasted image 20250820152513.png]]

> Работает он по принципу Last in First out(как магазин у пистолета): 
> Если продолжить выполнение программы, то будет ошибка StackOverflow

![[Pasted image 20250820151758.png]]

> Нужно всегда предусматривать выход из рекурсии, чтобы избежать ошибки.

### Пример рекурсии с предусмотренным методом.

```csharp
        static void Foo(int i )
        {
            Console.WriteLine(i);
            if (i >= 3)
                return;
                
            i++;
            Foo(i);
        }
        static void Main(string[] args)
        {
            Foo(0);
        }
```

> В стеке можно отследить откуда был вызван метод, и перенестись на то самое место кода.

![[Pasted image 20250820154616.png]]

### 🔹 Недостатки рекурсии

1. **Высокое потребление памяти**
- Каждый вызов метода добавляется в стек → при больших глубинах можно получить `StackOverflowException`.
1. **Медленнее, чем итеративные решения**
- Из-за накладных расходов на вызовы функций.

1. **Сложнее отлаживать и понимать при глубокой вложенности**
- Особенно если рекурсивные вызовы зависят друг от друга.


### 🔹 Плюсы рекурсии

1. **Простота и наглядность кода**
- Легко читать и писать, особенно для задач с повторяющейся структурой (факториал, числа Фибоначчи, обход деревьев).

1. **Подходит для задач с естественной иерархией**
- Деревья, графы, вложенные структуры, обход файловой системы.

1. **Меньше кода, чем с циклами**
- Иногда одно рекурсивное выражение заменяет сложный цикл с условием.




