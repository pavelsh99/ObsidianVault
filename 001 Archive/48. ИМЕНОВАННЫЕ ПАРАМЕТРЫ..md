---
tags:
  - csharp
date: 2025-08-18
---

## **Именованные параметры в C#**

**Именованные параметры** позволяют вызывать метод, указывая **имена аргументов**, а не только их порядок.  

Это повышает читаемость кода и даёт возможность передавать аргументы в любом порядке.

### Cинтаксис:
```csharp

Метод(имяПараметра: значение, другоеИмя: значение);

```

```csharp
static int Sum(int a, int b, bool enablelogging = false)
{
    int result = a + b;

    if (enablelogging)
    {
        Console.WriteLine("Значение переменной a = " + a);
        Console.WriteLine("Значение переменной b = " + b);
        Console.WriteLine("Результат сложения = " + result);
    }

    return result;
}

static void Main(string[] args)
{
    int firstvalue = 5;
    int secondvalue = 2;

    int result = Sum(b: secondvalue, a: firstvalue, enablelogging : true);//тут
    
    Console.WriteLine(result); // 7
}

```

### 📌 Главное для конспекта:

- Именованные параметры → можно указывать аргументы по имени, а не по порядку.

- Удобно, когда много параметров и они одного типа.

- Работают вместе с необязательными параметрами.

### 🔹 Особенности

1. Можно **менять порядок аргументов**:
```csharp
Sum(a: 2, b: 5);     // 7
Sum(b: 5, a: 2);     // 7
```
1. Можно использовать вместе с **необязательными параметрами**
```csharp
Sum(a: 3, b: 7, enablelogging: true);
```
1. Именованные параметры повышают **читаемость**, особенно когда много аргументов одного типа:
```csharp
// непонятно
Configure(true, false, 10, 50);

// понятно
Configure(enableCache: true, enableLogging: false, timeout: 10, retryCount: 50);

```