---
tags:
  - ключевое_слово
  - csharp
date: 2025-08-16
---
[КЛЮЧЕВОЕ СЛОВО PARAMS | C# ОТ НОВИЧКА К ПРОФЕССИОНАЛУ | # 46](https://www.youtube.com/watch?v=gwYuiwelSQs&list=PLQOaTSbfxUtD6kMmAYc8Fooqya3pjLs1N&index=64)

## Для чего нужен `params`

`params` — это модификатор, который позволяет методу принимать **произвольное количество аргументов** одного типа.  
Вместо того чтобы передавать массив вручную, ты можешь перечислить значения через запятую.
#### Пример без `params` (неудобный вариант)

```csharp
static int Sum(int[] numbers)
{
    int result = 0;
    foreach (int n in numbers)
        result += n;
    return result;
}

static void Main()
{
    int sum = Sum(new int[] { 1, 2, 3, 4 }); // нужно создавать массив руками
    Console.WriteLine(sum); // 10
}
```

#### Пример с `params` (удобный вариант)

```csharp
static int Sum(params int[] numbers)
{
	int result = 0;
	
	foreach (int number in numbers)
	    result += number; 
    return result;
       
}
static void Main(string[] args)
{
    int s1 = Sum(1, 2, 3);       //Можно перечислить
    int s2 = Sum(20, 30, 40, 50);//любое колличество аргументов
    int s3 = Sum();              //Даже пустой вызов. (вернет 0)    
    
    Console.WriteLine(s1); // 6
    Console.WriteLine(s2); // 140
    Console.WriteLine(s3); // 0
}
```

### Как это работает внутри

- На самом деле метод всё равно получает **массив** (`int[]`).

- `params` просто позволяет тебе **не создавать массив руками**.

- Ты можешь вызвать `Sum(new int[] { 1, 2, 3 })`, и это будет то же самое, что `Sum(1, 2, 3)`.


### Главное, чтобы вспомнить:

1. `params` всегда указывается **последним параметром** метода.

2. С помощью `params` можно передавать **любое количество аргументов одного типа**.

3. Без `params` приходится вручную создавать массив, что неудобно.

### Бонус

```csharp
static void Foo(params object[] parametrs)
{
    string message = "Тип данных {0}, значение {1}";

    foreach (var items in parametrs)
        Console.WriteLine(message, items.GetType(), items);
}

static void Main(string[] args)
{
    Foo('a', 146, "hello", 12.323f, 666, "No", -2);
}

```

#### 🔹 Что тут происходит

1. `Foo` принимает **любое количество аргументов** (`params object[]`).
- Так как тип `object`, можно передать значения **любого типа**: числа, строки, символы, float и т. д.
- В методе они собираются в массив `object[]`.
2. `message` — это шаблон для форматированной строки:

- `{0}` заменяется на тип данных (`items.GetType()`),

- `{1}` — на само значение (`items`).

3. `foreach (var items in parametrs)` перебирает все переданные аргументы.

4. `Console.WriteLine(message, items.GetType(), items)` выводит тип и значение каждого аргумента

##### Результат

```csharp
Тип данных System.Char, значение a
Тип данных System.Int32, значение 146
Тип данных System.String, значение hello
Тип данных System.Single, значение 12,323
Тип данных System.Int32, значение 666
Тип данных System.String, значение No
Тип данных System.Int32, значение -2
```

#### Главное

- `params object[]` — мощный способ принимать любое количество параметров разных типов.

- `GetType()` показывает реальный тип данных объекта.

- Такой код демонстрирует гибкость `params` и то, что в .NET всё наследуется от `object`.